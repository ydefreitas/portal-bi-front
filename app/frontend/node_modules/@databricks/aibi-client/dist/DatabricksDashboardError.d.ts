export declare enum ErrorCode {
    INSTANCE_DESTROYED = "INSTANCE_DESTROYED",
    INVALID_TOKEN_FORMAT = "INVALID_TOKEN_FORMAT",
    IFRAME_NOT_LOADED = "IFRAME_NOT_LOADED",
    IFRAME_NOT_LOADED_TOKEN_EXPIRED = "IFRAME_NOT_LOADED_TOKEN_EXPIRED",
    TOKEN_EXPIRED_NO_REFRESH_METHOD = "TOKEN_EXPIRED_NO_REFRESH_METHOD",
    INVALID_CONFIG_VERSION = "INVALID_CONFIG_VERSION",
    NOT_SUPPORTED = "NOT_SUPPORTED",
    REQUEST_TIMED_OUT = "REQUEST_TIMED_OUT",
    REQUEST_FAILED = "REQUEST_FAILED",
    /**
     * The dashboard rejected the request as invalid — the caller passed something it
     * couldn't accept (e.g. an unknown filter ID, or a value that doesn't match the
     * filter's type). Actionable by the host: fix the input and retry. Modeled
     * separately from the internal {@link ErrorCode.REQUEST_FAILED}.
     */
    INVALID_REQUEST = "INVALID_REQUEST"
}
export declare class DatabricksDashboardError extends Error {
    readonly errorCode: ErrorCode;
    /**
     * @param detail - Extra context appended to the standard message, used when
     * the underlying failure is only known at runtime (e.g. text forwarded by the
     * embedded dashboard).
     */
    constructor(errorCode: ErrorCode, detail?: string);
    private static getMessage;
}
//# sourceMappingURL=DatabricksDashboardError.d.ts.map